swagger: "2.0"
info:
  version: "0.0.1"
  title: "Minimal API Sepcification"
paths:
  /greetings/hello:
    get:
      operationId: "hello"
      parameters: []
      responses:
        200:
          description: "succeeded"
          schema:
            type: "string"
            example: "hello"
    post:
      operationId: "hello with name"
      parameters: []
      responses:
        200:
          description: "succeeded"
          schema:
            type: "string"
            example: "hello niku"
